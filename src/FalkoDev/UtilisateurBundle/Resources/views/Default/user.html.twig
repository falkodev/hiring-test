{% extends 'FalkoDevUtilisateurBundle:Default:layout.html.twig' %}
{% form_theme form 'FalkoDevUtilisateurBundle:Default:bootstrap_3_horizontal_layout.html.twig' %}
    {% block body %}
            <h3>Formulaire de {{ type }} d'utilisateur</h3>
            <div class="well">
                {{ form(form) }}
                {% if message is defined %}
                   {{ message }}
                   <a href="{{ path('supprimerUtilisateur', {'id':utilisateur.id}) }}" class="btn btn-danger" onclick="document.forms['form'].submit(); return false;">Confirmer</a><br>
                {% endif %}
                <a href="{{ path('liste') }}" class="btn btn-default"><i class="glyphicon glyphicon-chevron-left"></i> Retour à la liste</a>
            </div>
    {% endblock %}

